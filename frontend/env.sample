# Copie para .env.local na pasta frontend:  cp env.sample .env.local
# Este ficheiro é versionado; .env*.local não vão para o Git.

# URL da API (proxy server-side em produção; local = backend Nest)
BACKEND_URL=http://localhost:3001
